<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <title>Registrati - Postit</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    <style>
        body { background: #f0f2f5; display: flex; align-items: center; min-height: 100vh; }
        .auth-card { width: 100%; max-width: 400px; margin: auto; padding: 2.5rem; border-radius: 15px; background: white; box-shadow: 0 10px 30px rgba(0,0,0,0.1); }
    </style>
</head>
<body>
    <div class="auth-card">
        <div class="text-center mb-4">
            <h2 class="fw-bold text-success"><i class="bi bi-person-plus-fill"></i> Unisciti</h2>
            <p class="text-muted">Crea il tuo profilo social</p>
        </div>
        <form action="/register" method="POST">
            <div class="mb-3">
                <label class="form-label small fw-bold">Scegli Username</label>
                <input type="text" name="username" class="form-control" placeholder="es: mario_rossi" required>
            </div>
            <div class="mb-4">
                <label class="form-label small fw-bold">Scegli Password</label>
                <input type="password" name="password" class="form-control" placeholder="min. 6 caratteri" required>
            </div>
            <button class="btn btn-success w-100 fw-bold py-2 mb-3">Registrati ora</button>
        </form>
        <div class="text-center">
            <span class="small">Hai già un account? <a href="/login" class="text-decoration-none">Vai al Login</a></span>
        </div>
    </div>
</body>
</html>