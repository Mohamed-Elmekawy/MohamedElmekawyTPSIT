<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Array in Box Div</title>
    <style>
        .box {
            border: 1px solid red;/*bordo 1 pixel di colore rosso*/
            padding: 10px;/*distanza del bordo dal testo contenuto*/
            margin: 5px;/*distanza delmargine di 5 px*/
            display: inline-block;
        }
    </style>
</head>
<body>

    <div id="container">
        <!-- Questo div conterrà i quadrati generati dal JavaScript -->
        esempio di stampa di un array in quadrati div nell'html
    </div>
    <script>
        // Funzione per stampare gli elementi di un array in div
        function printArrayInDivs(arr) {
            const container = document.getElementById('container');
            
            // Per ogni elemento dell'array, crea un div, aggiungi la classe 'box' e il testo, poi aggiungilo al container
            arr.forEach(element => {
                const div = document.createElement('div');
                div.classList.add('box');
                div.textContent = element;
                container.appendChild(div);
            });
        }

        // Esempio di utilizzo della funzione con un array di numeri e stringhe
        const myArray = [1, 2, 3, 4, 5, "ciao", "test"];
        printArrayInDivs(myArray);
    </script>
</body>
</html>
